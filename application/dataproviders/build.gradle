dependencies {
    compile project(":application:core")
    compile libs.rest
    compile libs.jpa
    compile libs.utils
    compile libs.log
    compile libs.sms
    testCompile libs.unit_tests
    testCompile libs.spock
}